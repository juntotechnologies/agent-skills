## This Project: personal-config

### Agent Content Recovery

This file is generated in `agent-skills` and linked in by its installer; it
isn't committed here. If it's missing, run `scripts/update_agent_files.sh`,
which clones or updates `agent-skills` and restores the link.
